import { Inter, Lusitana, New_Amsterdam } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] })
export const new_amsterdam = New_Amsterdam({weight: ['400'], subsets: ['latin']})
export const lusitana = Lusitana({weight: ['400'], subsets: ['latin'] })